export enum AppRoute {
  Main = '/',
  About = '/about',
  UserCard = '/user/:id',
}

export enum NameSpace {
  Posts = 'POSTS',
  User = 'USER',
}

export enum APIRoute {
  Posts = '/posts',
  Comments = '/comments',
  Users = '/users',
}